{
  "name": "trail-pocket-pwa",
  "version": "3.1.0",
  "private": true,
  "type": "module",
  "description": "Local-first hiking routes with selectable offline maps, search and path routing",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "@xmldom/xmldom": "0.9.12",
    "fake-indexeddb": "6.2.5"
  }
}
